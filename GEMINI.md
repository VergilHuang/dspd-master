# AI 協作規範

每次處理程式碼後不要自動執行測試, 除非有特別要求, 否則不要自動執行 npm run codecept.

# 開發原則

- 避免過度設計，盡量以低偶合的模式處理資料流。 Custom Hooks 應該只用在有跨元件重複使用的功能。 避免將所有的 Handler 跟 States 都存放在 Hooks 裡面。 非涉及跨元件的邏輯應該只在單一元件內處理。

- 避免不必要的 prop drilling

- 注重職責分離

# 程式碼風格
